

{beyond compare 4你的30天评估期到期怎么解决
	3.删除C:\Users\Administrator\AppData\Roaming\Scooter Software
	4. 永久使用需删除目录下所有：C:\Users\Administrator\AppData\Roaming\Scooter Software\Beyond Compare 4
	5. rd /S /Q "C:\Users\Administrator\AppData\Roaming\Scooter Software\Beyond Compare 4"，保存为beyondcompareReset.bat,运行就行了
	
	一劳永逸，修改注册表
		1)在搜索栏中输入 regedit  ，打开注册表
		2) 删除项目：计算机\HKEY_CURRENT_USER\Software\ScooterSoftware\Beyond Compare 4\CacheId
}
{
	在线ubuntu	http://c-ide.csdn.net/ws/qtvggsbg
}

{通过web录屏
	https://www.apowersoft.cn/free-online-screen-recorder
}
http://blog.chinaunix.net/uid-22145625-id-3241368.html	多级makefile
常用背景色：x002B36

{micorsoft 账号
	naypesong@foxmail.com
	liu?15928876719
}
Administrator

song990454

Source Insight常用的快捷键：
{
	Ctrl+= :Jump to definition
	Alt+/ :Look up reference
	F3 : search backward
	F4 : search forward
	F5: go to Line
	F7 :Look up symbols
	F8 :Look up local symbols
	F9 :Ident left
	F10 :Ident right
	Alt+, :Jump backword
	Alt+. : Jump forward
	Shift+F3 : search the word under cusor backward
	Shift+F4 : search the word under cusor forward
	F12 : incremental search
	Shift+Ctrl+f: search in project
	shift+F8 : hilight word
	
	重画屏幕 : Ctrl+Alt+Space
	复制一行 : Ctrl+K
	剪切一行 : Ctrl+U
	替换 : Ctrl+H
	转到下一个文件 : Ctrl+Shift+N
	到文件底部 : Ctrl+End,
	到一行的尾部 : End
	转到行 : F5, Ctrl+G
	转到下一个修改 : Alt+(KeyPad) +
	回到前一个修改 : Alt+(KeyPad) -
	
	
	退出程序                                : Alt+F4  
		重画屏幕                                : Ctrl+Alt+Space  
		完成语法                                : Ctrl+E  
		复制一行                                : Ctrl+K  
		恰好复制该位置右边的该行的字符          : Ctrl+Shift+K  
		复制到剪贴板                          : Ctrl+Del  
		剪切一行                                : Ctrl+U  
		剪切该位置右边的该行的字符              : Ctrl+;  
		剪切到剪贴板                          : Ctrl+Shift+X  
		剪切一个字                              : Ctrl+,  
		左边缩进                                : F9  
		右边缩进                                : F10  
		插入一行           : Ctrl+I  
		插入新行                                : Ctrl+Enter  
		加入一行                                : Ctrl+J  
		从剪切板粘贴                          : Ctrl+Ins  
		粘贴一行                                : Ctrl+P  
		重复上一个动作                       : Ctrl+Y  
		重新编号                                : Ctrl+R  
		重复输入                                : Ctrl+\  
		替换                                    : Ctrl+H  
		智能重命名                              : Ctrl+''  
		关闭文件                                : Ctrl+W  
		关闭所有文件                          : Ctrl+Shift+W  
		新建        :Ctrl+N  
		转到下一个文件                      : Ctrl+Shift+N  
		打开                                : Ctrl+O  
		重新装载文件                        : Ctrl+Shift+O  
		另存为                              : Ctrl+Shift+S  
		显示文件状态                        : Shift+F10  
		激活语法窗口                        : Alt+L  
		回到该行的开始                      : Home  
		回到选择的开始                      : Ctrl+Alt+[  
		到块的下面                             : Ctrl+Shift+]  
		到块的上面                             : Ctrl+Shift+[  
		书签                                   : Ctrl+M  
		到选择部分的尾部                       : Ctrl+Alt+]  
		到下一个函数                           :小键盘 +  
		上一个函数                              :小键盘 -  
		后退                                    : Alt+,, Thumb 1 Click  
		后退到索引                              : Alt+M  
		向前                                    : Alt+., Thumb 2 Click  
		转到行                                  : F5, Ctrl+G  
		转到下一个修改                          : Alt+(KeyPad) +  
		转到下一个链接                          : Shift+F9,  
		回到前一个修改                          : Alt+(KeyPad) -  
		跳到连接(就是语法串口列表的地方)        : Ctrl+L  
		跳到匹配                                : //Alt+ ]  
		选择一行                                : Shift+F6  
		选择句子(直到遇到一个 . 为止)           : Shift+F7, Ctrl+.  
		从当前位置选择到文件结束                : Ctrl+Shift+End  
		从当前位置选择到行结束                  : Shift+End  
		从当前位置选择到行的开始                : Shift+Home  
		从当前位置选择到文件顶部                : Ctrl+Shift+Home  
		选择一个单词                            : Shift+F5  
		选择左边单词                            : Ctrl+Shift+Left  
		选择右边单词    : Ctrl+Shift+Right  
		排列语法窗口(有三种排列方式分别按1,2,3次)           : Alt+F7  
		移除文件                                : Alt+Shift+R  
		同步文件                                : Alt+Shift+S  
		增量搜索(当用Ctrl + F 搜索,然后按F12就会转到下一个匹配)         : F12  
		替换文件                                : Ctrl+Shift+H  
		向后搜索                                : F3  
		在多个文件中搜索                        : Ctrl+Shift+F  
		向前搜索                                : F4  
		搜索选择的(比如选择了一个单词,shift+F4将搜索下一个)       : Shift+F4  
		快捷键集锦：  
		Ctrl+= :Jump to definition  
		Alt+/ :Look up reference  
		F9 :Jump to next reference  
		F7 :Look up symbols  
		F8 :Look up local symbols  
		F9 :Ident left  
		F10 :Ident right  
		Alt+, :Jump backword  
		Alt+. : Jump forward  
		F2: switch to next page  
		F3 : search backward  
		F4 : search forward  
		F5: Jump to line ?  
		Ctrl+,/Ctrl+. Jump to last/past  
		Shift+F3 : search the word under cursor backward  
		Shift+F4 : forward  
		F12 : incremental search  
		Shift+Ctrl+f : search in project  
		Others(Common in Windows system)  
		Shift+Tab: Moving foward(After text selected)  
		Tab:Moving backward(After text selected)  
		Shift+(Up,Down,Left,Rght):Select code,do not have to using mouse cursor  
		   
		1.sourceinsight　screen font　的默认字体是Verdana的，它是一直变宽字体。在Document style中可以将字体改为定宽的Courier   
		2. 勾掉indent Open Brace和Indent Close Brace的效果: 继上一段，在相对缩进行里, 如果输入"{"或"}", 则自动和上一行列对齐   
		3.今天把一个用sourceinsight排版整齐的C文件，偶然用VC打开一看，全乱了。研究了半天，发现SI对每个字符的宽度不太一致。发现选上"view --> draft view"，
		就可以让每个字符的宽度一致了。快捷键是 "Alt + F12"  
		4.选中几行代码按tab键或者shift+tab可以左右移动代码，调整代码时很有用。  
		//------------------------------------------------------------------  
		配置成简单好用的c/java代码编辑器  
		　　1、缩进与tab  
		　　（1）Options菜单PreferencesTyping卡，勾掉下面两项∶  
		　　Typing tab indents line，regardless of selection，空行按tab无法前进  
		　　Typing tab replaces current selection，选定部分内容、再按tab时会清除所选  
		　　（2）Options菜单Document Options（针对不同文件类型，分别进行设置）下拉左上文件类型框、选择合适类型（c源文件）Editing Options框中，tab width=2Editing Options框中，
		勾选Expand tabs（这样，按tab键、等价于输入2个空格）  
		　　（3）Options菜单Document Options选择合适的文件类型点击右边中间的Auto Indent钮在弹出的框中，左边一定要点Smart，右边有两个复选框Indent Open Brace和Indent Close Brace，
		具体效果可以看SIS的HELP。按照部门里的编程风格要求，最方便的就是把两个复选框都取消掉，然后点OK。  
		　　勾选Auto Indent之SMART的效果∶在C程序里, 如果遇到行末没有分号的语句,如IF, WHILE, SWITCH等, 写到该行末按回车，则新行自动相对上一行缩进两列。  
		　　勾掉Indent Open Brace和Indent Close Brace的效果∶继上一段，在相对缩进行里, 如果输入"}", 则自动和上一行列对齐（好像勾不勾都会有这个功能）；
		而输入"{"时，不会与下面的行对齐（这是勾上Indent Open Brace时的效果）。  
		　　2、向项目中添加文件时，只添加特定类型的文件（文件类型过滤器）  
		　　有个同事比较生猛，得整汇编代码，但在SIS里建立PROJECT并ADD TREE的时候，根据默认设置并不会把该TREE里面所有汇编文件都包含进来，只加了.inc和.asm后缀的，.s后缀的没有。而且用SIS打开.s的文件，
			一片黑白没有色彩，感觉回到DOS的EDIT时代了……解决方法是在Options->Document Options里面，点左上的Document Type下拉菜单，选择x86 Asm Source File，然后在右边的File filter
			里*.asm；*.inc；的后面加上*.s；
			接着CLOSE就可以了。上面两个问题解决了，但注意加入*.s后还需要重新ADD TREE一遍才能把这些汇编加到PROJECT里面。  
		　　3、去掉功能强大但是无用的自动完成功能  
		　　Options菜单Preferences Typing卡Auto Completion框，勾掉Use automatic symbol completion window（这里是SIS的全局设置）  
		　　Options菜单Document OptionsEditing Options框中，勾掉Allow auto-complete（局部设置）  
		　　上面两项必须全部勾选，才能启用Auto Completion功能  
		　　4、恢复小键盘的“+，-，*，/”功能  
		　　Options菜单Key assignments，通过关键词Scroll 找到Scroll Half Page Up，取消小键盘/；通过关键词Scroll 找到Scroll Half Page Down取消小键盘*；通过关键词Function找到Function Up，
		取消小键盘-，通过关键词Function找到Function down，取消小键盘+。  
		　　5、恢复ctrl+a的全选功能  
		　　通过关键词save 找到save all，更改为ctrl+shift+a，通过关键词select找到select all，更改为ctrl +a  
		　　6、解决字符等宽对齐问题。  
		　　SIS默认字体是VERDANA，很漂亮。这网页上应该也是用的VERDANA字体。但由于美观的缘故，VERDANA字体是不等宽的。比如下面两行  
		　　llllllllll  
		　　MMMMMMMMMM  
		　　同样10个字符，长度差多了.用VERDANA来看程序，有些本应该对齐的就歪了。解放方法是使用等宽的字体，但肯定比较丑。可以用DOS字体，也就是记事本里的默认字体sysfixed 很丑，
		要有心理准备。比较推荐的是用Courier New。  
		　　SourceInsight提供的功能  
		　　1、解析日志信息时非常有用的Source Link  
		　　总地说来，SourceLink根据特定的搜索模式，把当前文件中满足模式的行、链接到由该行指定的其他源文件中。  
		　　所谓特定的搜索模式，共有两种“File, then line”和“Line, then file”，其中前后两部分依靠正则表达式的组的概念来予以分割。如果当前文件具有匹配行，比如“Error d:tcsrcq5.c 18: 
		Lvalue required in function jsSort”，那么SourceInsight在该行创建SourceLink、把该行链接到由该行指定的文件中（即d:tcsrcq5.c，第18行）。  
		　　1.1 创建SourceLink  
		　　运行Search菜单的Parse Source Links…命令，在弹出的框中、选择搜索模式、并填入相应的正则表达式串，点OK，SIS就会解析当前文件，如果有匹配，就创建SourceLink。  
		　　1.2 在解析日志信息时，使用SourceLink  
		　　可以打开日志信息，运行Parse Source Links命令，日志中能够匹配模式的每一行（通常是含有错误信息的行）、就会被设置上一个SourceLink  
		　　1.3在解析自定义命令输出时，使用SourceLink  
		　　首先勾选Custom Command 中的“Parse Links in Output”，然后选择特定的搜索模式，最后填入合适的正则表达式。这样，Source Insight把输出信息作为当前搜索用文件；
		并且，如果有匹配行（通常即编译错误信息行），SIS为该行创建SourceLink、并把每一个错误信息中给定的文件（和行号）作为link目的地，这对于我们修改源代码错误非常有帮助。  
		　　2、替换（Replace）VS 上下文敏感的智能重命名（Context-Sensitive Smart Rename）  
		　　2.1 替换（Replace）  
		　　目前来说，普通的替换命令、快捷键为ctrl+H，足以已满足工作要求。  
		　　在弹出的替换窗口中，在Search框中勾选Selection来只在所选文本区域中替换（当然这时你要先选定区域然后再按ctrl+H）、勾选WholeFile来在整个当前文件内替换、
		两者都不勾选来从当前光标处替换至文件末尾；点右边的Files…按钮，可选择替换多个文件的内容。  
		　　2.2上下文敏感的智能重命名（Context-Sensitive Smart Rename）  
		　　Smart Rename命令、快捷键是Ctrl+’，是上下文敏感的全局搜索替换。
		它可以智能地重命名全部项目文件中的一个标示符。SourceInsight的搜索索引（search index）使得搜索过程进行地非常快。而且，使用Smart Rename所做的替换会被记录在Search Results窗口中，
		每一条替换记录旁有一个SourceLink链接到替换发生地文件。  
		　　Smart Rename可以用来重命名标记（symbol）。如果勾选了Smart Reference Matching选项，Smart Rename就只在正确的上下文范围内进行重命名。
		它可以智能地重命名全部项目文件中的一个标示符；它可以重命名函数本地变量，类或结构体成员、函数。  
		　　在弹出的Smart Rename窗口中有下面几项∶  
		　　Old Name填旧名称。光标下的词会被自动加载；光标的位置非常重要，这是因为Source Insight会根据本地上下文背景、准确地确定你想要重命名哪一个标记。  
		　　推荐只填单个词、而不是字符串。  
		　　如果你在命名成员变量、或本地变量（），Old Name框中会显示完全标记名、即上层容器名+标记名。例如，框中的“DocDraw.paintStruc”代表DocDraw是函数名，paintStruc是函数的本地成员变量。  
		　　New Name填新名称。只填标记名，不填上层容器名。  
		　　Output Search Results如果勾选，搜索替换结果日志会被输出到Search Results窗口中。可以通过Windows菜单来切换，或ctrl+tab切换察看。并且每一条记录旁会有SourceLink链接到替换发生地文件。  
		　　Confirm Each Replacement每次替换询问。  
		　　Skip Comments不重名注释部分。  
		　　【使用心得列表】  
		　　（１）如何用Smart Rename重命名数组的数组名？如果只选取数组名，会报错！  
		　　（２）如果勾掉Smart Reference Matching，会搜索全部项目文件，并且Old Name框中不显示完全限定名；如果勾选Smart Reference Matching，无法重命名数组名，而且鼠标位置不正确时会报错。应该如何应对？  
		　　3、在SourceInsight中提供的正则表达式  
		　　3.1在SourceInsight中提供的正则表达式  
		　　正则表达式，是用来匹配复杂模式的特殊搜索用字符串。正则表达式串中，许多字符具有特殊的含义。例如，有个特殊的字符代表“行首”。  
		　　下面是SourceInsight提供的所有可用特殊字符∶  
		　　Table 4.3: Regular Expression Characters  
		　　Character Matches  
		　　^ (at the beginning only) beginning of line 。如^Hello，匹配Hello在句首。  
		　　. any single character  
		　　[abc] any single character that belongs to the set abc  
		　　[^abc] any single character that does not belong to the set abc  
		　　* zero or more occurrences of the precedingcharacter  
		　　+ one or more occurrences of the precedingcharacter  
		　　t a tab character  
		　　s a space character  
		　　w white space (a tab or a space character)  
		　　$ the end of the line 。如TRUE$，匹配TRUE在句尾。  
		　　转义字符。如果在它后面有元字符，取消其特殊含义。 　　  
		　　可利用“(”和“)”、把正则表达式分割成不同的组；模式中的每个组自左向右指定为Group #n，n=1,2,…；组的概念在替换时很有用。  
		　　例如∶  
		　　abc(xyz)可匹配abcxyz，其中xyz被认为是group#1，  
		　　利用21来替换(abc)(xyz)，替换结果为xyzabc。  
		  
		　　3.2 正则表达式在配置tc编译器中的应用∶  
		　　正则表达式格式与源代码文件路径相对应，这里我的tc安装目录为d:tc，tc源文件放在d:tcsrc下，并命名为qn.c或qtn.c（其中n=1,2,…）。  
		　　观察Tc编译器某一次输出错误信息的格式∶  
		　　Error d:tcsrcq5.c 18: Lvalue required in function jsSort  
		　　则我们要匹配“d:tcsrcq5.c 18”部分，进一步地，按照SourceInsight捕捉输出并加以解析时的要求，要以组的形式、分别匹配“d:tcsrcq5.c 18”中的文件部分和行号部分∶  
		　　行号([1-9][0-9]*)  
		　　空格行号s([1-9][0-9]*)  
		　　文件名(d:tcsrc[qQ][tT][1-9][0-9]*.[cC])  
		　　全部加起来为∶  
		　　(d:tcsrc[qQ][tT]*[1-9][0-9]*.[cC])s([1-9][0-9]*)  
		　  
		　　3.3 正则表达式在配置javac编译器中的应用∶  
		　　我的JAVA_HOME是c:jdk，我的java源文件放于d:javasrc中，并命名为qn.java或qtn.java（其中n=1,2,…）。  
		　　观察JDK编译器某一次输出错误信息的格式∶  
		　　D:javasrcQ3.java:3: ';' expected  
		　　正则表达式为∶  
		　　([dD]:javasrc[qQ][tT]*[1-9][0-9]*.java):([1-9][0-9]*) 　  
		　　4、自定义命令  
		　　自定义命令与项目相关，在一个项目中定义的所有自定义命令属于该项目、只对该项目有效（包括快捷键等）。  
		　　自定义命令类似于命令行批处理文件。SIS允许自定义命令在后台运行；并可以捕捉自定义命令的输出、放置于文件中、或粘贴入当前光标插入点。  
		　　分别利用上面SIS对输出信息的处理方式，自定义命令对集成编译器相当有用，可以捕捉编译器输出并创建SourceLink寻错；自定义命令对于文本过滤也相当有用，可选中待过滤区块、运行Sort自定义命令、
		粘贴回选定区块、即完成文本过滤。  
		　　请按下面步骤创建自定义命令∶  
		　　Options菜单Custom Command  
		点右边Add钮、填入新自定义命令名称，或下拉左边Commands、选择命令进行修改  
		Run框、填入待执行命令行，可含有特殊元字符，见后面的元字符表 Dir框、执行命令行时应处的目录，如不填，以源代码文件所在目录为命令执行目录  
		勾选Output框的Capture Output、输出被捕捉，如果勾选Paste Output，输出被粘贴  
		勾选Control Group框中的Save Files First、SIS会在运行命令前先检查文件是否保存  
		勾选Control Group框中的Pause When Done、SIS会在命令结束后暂停、方便检查  
		勾选Source Links in Output框中的Parse Source Links，
	
	{error
		一般来说进到source insight的安装目录进入Settings这个文件下，把GLOBAL.CF3删掉就行了
		
		改变字体大小
			ALT+T：在弹出的对话框选择Arial 12
		改变背景色
			options->preferences...->colors->window background,color(想要的颜色)
	}
	
}

eclipse快捷键大全
{
	{查看函数被哪些调用了，含有原函数
		Ctrl+Alt+h
	}
	
	{查看函数被哪些调用了，不含原函数
		Ctrl+Shift+g
	}
	
	{常用快捷键
		{可以快速查找本文档的函数
			Ctrl+o
		}
		
		{添加注释
			Ctrl+/,取消就是再按一下
		}
		
		{删除当前行
			Ctrl+D
		}
		
		{窗口最大化和还原
			Ctrl+M
		}
	}
	
	{查看和定位快捷键
		{快速向下和向上查找选定的内容，从此不再需要用鼠标单击查找对话框了。  
			【Ctrl+K】、【Ctrl++Shift+K】，相当于source insight的F3 F4
		}
		
		{整个工程查找函数
			Ctrl+Shift+T
		}
		
		{工程中查找文件
			Ctrl+Shift+R
		}
		
		{查找类、方法和属性的引用。例如要修改引用某个方法的代码，可以通过这个快捷键迅速定位所有引用此方法的位置。
			Ctrl+Shift+G
		}
		
		{格式化代码
			Ctrl+Shift+F
		}
		
		{多功能键
			ALT+Shift+W，会弹出几个选项，
			Problem Details:当前问题详细说明
			Include Browser:包含有哪些头文件
			C/C++ Projects:当前文件的路径和函数
			Project Explorer:感觉跟前一个是一样的
			Outline:跟Ctrl+O差不多的，
			Navigator:当前文件的路径和函数
			Properties:当前函数名字
		}
		
		{定位到当前编辑器的某一行
			Ctrl+L
		}
		
		{后退历史和前进历史
			【Alt+←】、【Alt+→】
		}
		
		{快速定位光标位置的某个类、方法和属性
		  【F3】
		}

		{显示类的继承关系，并打开类继承视图。
			【F4】
		}
	}
	
	{调试快捷键

	  Eclipse中有如下一些和运行调试相关的快捷键。

	  1. 【Ctrl+Shift+B】：在当前行设置断点或取消设置的断点。

	  2. 【F11】：调试最后一次执行的程序。

	  3. 【Ctrl+F11】：运行最后一次执行的程序。

	  4. 【F5】：跟踪到方法中，当程序执行到某方法时，可以按【F5】键跟踪到方法中。

	  5. 【F6】：单步执行程序。

	  6. 【F7】：执行完方法，返回到调用此方法的后一条语句。

	  7. 【F8】：继续执行，到下一个断点或程序结束。
	}
	
	{其他快捷键
		1. 【Ctrl+F6】：切换到下一个编辑器。

		2. 【Ctrl+Shift+F6】：切换到上一个编辑器。

		3. 【Ctrl+F7】：切换到下一个视图。

		4. 【Ctrl+Shift+F7】：切换到上一个视图。

		5. 【Ctrl+F8】：切换到下一个透视图。

		6. 【Ctrl+Shift+F8】：切换到上一个透视图。
	}
	
	{Ctrl系列
		{重命名所有相同的变量
			Ctrl+1
		}
		
		{快速显示当前Editer的下拉列表
			Ctrl+E 
		}
		
		{显示管理当前打开的所有的View的管理器
			Ctrl+Shift+E 
		}
		
		Ctrl+Q 定位到最后编辑的地方
		
		Ctrl+H:打开搜索窗口
		
		Ctrl+/(小键盘) 折叠当前类中的所有代码	//测试中，这个有用，下个那个没用的

		Ctrl+×(小键盘) 展开当前类中的所有代码
	}
}

{vistual studio
	Ctrl+G	//跳到指定行数
	
	使用快捷键Ctrl+F3可以跳转到下一个相同的词。按Shift+F3可以往上查找
	
	格式化部分代码：选中代码->Ctrl+K,F。或者Ctrl+E,F。
	
	在vistual studio下建C工程
	file->new project->visual c++->win32->win32 console application
		next-> 勾上empty project->finish
		在resource files下添加new item->C++file
		把这个文件的后缀.cpp改为.c


	visual studio 编译C程序出错时怎么办


	 项目-->工程属性->配置属性-> 清单工具->输入和输出->嵌入清单，选择[否]   ----------->只要设这个就可以了(在2010上值改了这个就可以了)

	项目-->项目属性-->配置属性-->清单工具-->使用FAT32解决方法为 是;（这个没到）；

	英文版的
		project->project_c propreties->manifest tool->input and output->embed manifest(change yes to no)确定就OK（当前就可以），应用是以后所有的都是这样

	编译的时候ctrl+F5

	在vistual studio下建C++工程
	file->new project->visual c++->empty project
		在resource files下添加new item->C++file

		cout<<"test project"<<endl;
		system("pause");//就是在命令行上输出一行类似于“Press any key to exit”的字，等待用户按一个键，然后返回。

	在vistual studio下建C#工程
	file->new project->visual c#->console application(下面名字自己取)
		Console.WriteLine("c# project");
				Console.ReadKey();//就是在命令行上输出一行类似于“Press any key to exit”的字，等待用户按一个键，然后返回。
		
			如果安装了，提示：This extension is already installed to all applicable products。在vistual studio 中tool中的extension manage中去卸载就行了
		安装VC助手时，安装了VC助手时，然后再C盘搜索VA_X.dll,然后将安装软件中的VA_X.dll,替换这个就行了

	
	添加已有文件夹：https://www.cnblogs.com/lsgxeva/p/8045150.html
}

{nodpad++
	在设置->语言格式设置
	主题：solarized用这个很不错
		前景色：R:131 G:148 B:150
		背景色：R:0 G:43 B:54
}

{QT Creator
	F4 在头文件和代码之间切换
	F2 在声明和实现之间切换
	Shift+F2    声明和定义之间切换
	Ctrl + k 打开定位器（locator）	//可以输入来查找文件
	Ctrl + Shift + f  打开全局搜索框（Ctrl + Shift一般会跟切换输入法冲突，所以建议将Ctrl + Shift都改为其他键）
	Ctrl + f  打开当前页面搜索框
	Ctrl + 鼠标滚轮 缩放代码
	Ctrl + / 注释或取消注释
	Ctrl+1         欢迎模式
	Ctrl+2        编辑模式
	Ctrl+3        调试模式
	Ctrl+4        项目设置模式
	Ctrl+5        帮助模式    
	Ctrl+6        输出模式
	Ctrl+Space    自动补全（貌似会和输入法的切换冲突）
	Alt + 回车 快速修复，如为头文件中的方法添加实现，或为实现添加头文件中声明，为字符串增加多语言支持等，试一下。
	Ctrl + Shift +r 右键菜单’Refactor -> Rename‘，查找替换当前工程的 特定字符，可以使类名或方法名，方便重构操作。
	Alt + 0	隐藏和显示项目工程
	
	Ctrl+I        自动对齐
	
	Ctrl+Shift+<         折叠代码块
	Ctrl+Shift+>         展开代码块
	Ctrl+[        跳到代码块的头部
	Ctrl+]        跳到代码块的尾部
	
	Ctrl+Shift+Tab	历史中下个打开的文件	
	Ctrl+Tab		历史中先前打开的文件	
	Ctrl+Ins		复制行	
	Ctrl+Shift+V	查看剪切板历史	
	
	Ctrl+Shift+Down	向下移动当前行
	Ctrl+Shift+Up	向上移动当前行	
	
	Ctrl+U			选择当前块，第二次按下选择父块	//可以选择相当于一行
	
	{调试
		F5: 继续；开始调试
		
		Ctrl+F6:运行到选择的函数
		Ctrl+F10:运行到行
		F11：单步调试，（进入）
		Shift+F11：单步出来
		F10，下一行
		Shift+F5:停止调试
		F9：断点
		F3：查找下一处
		Shift+F3:查找上一处
	}
	编译更快：
		工具-》选项->构建套餐->自动检测下面那个，点一哈->Enviroment->Change->(MAKEFLAGS=-j4)
		tool->options->Build & Run->Kits->Auto-detected(click follow this)->Environment->Change->(MAKEFLAGS=-j4)
		（option）		MAKEFLAGS=-j4
	
}